name: Temporary refresh Knowledge Tree indexes batch 3

on:
  push:
    branches:
      - knowledge-tree/curated-population-batch-3
    paths:
      - registry/.refresh_indexes_batch3

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: knowledge-tree/curated-population-batch-3
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -r requirements-dev.txt
      - run: python scripts/validate_tree.py
      - run: python scripts/build_indexes.py
      - run: python scripts/build_indexes.py --check
      - run: python scripts/check_links.py
      - run: pytest -q
      - name: Commit generated indexes
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet -- indexes; then
            echo "Indexes already current"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add indexes
          git commit -m "chore: refresh deterministic Knowledge Tree indexes for batch 3"
          git push origin HEAD:knowledge-tree/curated-population-batch-3
